#extend("base"):
    #export("body"):
        <h1>Genres</h1>
            #for(genre in genres):
            <div class="row">
                <h4>#(genre.name)</h4>
            </div>
            #endfor
    #endexport
#endextend